/*
 * This is multi line comment
 */
/*
print "Hello, world!";

var a = 1;
while (a < 10) {
  print a;
  a = a + 1;
}

print "hi";
*/
(2+3)*(4-2);

// Comma operator
2+2, 1+1;

// Ternary operator

// true ? 1 : 2
// TODO: For some reason it stops building expressions after the first line, maybe this is because we don't have statements yet
false ? 1 : (2==3 ? 2 : 3)
